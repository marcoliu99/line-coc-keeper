[mypy]
python_version = 3.11
ignore_missing_imports = true
cache_dir = /tmp/coc-mypy-cache

# LINE's generated SDK types expose optional clients without useful narrowing;
# the adapter is exercised by runtime integration and unit-test coverage.
[mypy-app.main]
ignore_errors = true
